# SMGR 更新日志

本文档记录 SMGR 的主要版本迭代与行为调整。版本号遵循语义化规则：

- `patch`：问题修复、样式或文案调整。
- `minor`：新增、删除功能或调整既有行为。
- `major`：不兼容的大范围重构。

## v1.11.1 · 2026-08-25

### 比赛类型筛选修复

- 投注页切换比赛日期、销售状态或刷新比赛数据后，会自动清除已经不在最新选项中的比赛类型，避免不可见的历史选择继续过滤比赛。
- 订单页切换日期、进度、状态等条件并取得新的订单集合后，同样清除已经消失的比赛类型，确保用户无需操作已隐藏的标签即可看到新条件下的订单。
- 仍存在于新选项集合中的已选比赛类型会继续保留；筛选在选项变化后的首轮渲染就使用有效选择，避免短暂空结果。

### 开发记录与验证

- 增加比赛类型选择收敛的共享逻辑、Research 与 Planning 记录，并覆盖全部保留、部分清理和全部清空场景。
- 92 项自动化测试、ESLint、Cloudflare 生产构建及桌面端和 390px 移动端浏览器回归通过；控制台无 warning/error。

## v1.11.0 · 2026-08-24

### 订单筛选统计

- 订单页筛选汇总新增“筛选总额”，统计当前筛选结果中全部订单的投入；原“筛选投入”改为“已支付”，只统计已经支付的订单金额。
- 筛选盈亏继续以已支付支出和已结账收入计算，未支付订单不会提前形成账面亏损；盈亏提示同步使用“已支付”口径。
- 汇总布局扩展为“筛选结果、筛选总额、已支付、筛选盈亏、状态说明”，桌面端使用五列，手机端使用两列并保持无横向溢出。

### 投注日期吸附

- 投注页日期折叠标题增加 sticky 定位，滚动到收起后的固定页头下方时持续显示，进入下一日期分组后自然替换。
- 日期标题使用页面背景色和独立层级，比赛卡片滚动时不会与日期、场次数及折叠操作重叠；桌面端与手机端保持一致。

### 开发记录与验证

- 增加本次订单筛选统计与日期吸附的 Research、Planning 记录，并补充筛选总额与已支付支出口径的自动化测试。
- 91 项自动化测试、ESLint、Cloudflare 生产构建及浏览器回归通过；已验证 1440px 桌面端和 390px 手机端布局、日期切换、吸附状态折叠交互及控制台错误。

## v1.10.2 · 2026-08-20

### 订单批量操作确认

- 收紧一键结账和一键支付 Popover 的摘要文本样式范围，避免操作区按钮文字被灰色摘要样式覆盖；主按钮“确认结账”和“确认支付”恢复为白色字体。

### 验证

- 自动化测试、ESLint、Cloudflare 生产构建及浏览器关键样式验证通过。

## v1.10.1 · 2026-08-20

### 比分对齐与赛果颜色

- 比赛主行改为“主队名称、主队进球、分隔符、客队进球、客队名称”五列对称布局，比分分隔符不再受两侧队名长度影响，始终位于卡片中轴。
- 半场比分拆为中央三列并与全场比分共用中轴；无比分时两侧进球列留空，`VS` 同样保持居中。
- 全场比分按主胜红、平局蓝、主负绿显示；半场比分按自身胜平负独立着色，并以 55% 透明度保持副行层级。
- 已停售比赛继续使用灰色队名和时间，但不再覆盖比分的赛果语义色；桌面双列、单列和手机端共用同一套对称比例。

### 开发与发布规范

- 根目录增加 `AGENTS.md`，明确 Research、Planning、实现、验证和版本发布流程；部署必须依次完成更新日志与版本、提交、推送和部署，仅提交或推送而不部署时无需更新日志。
- Research 与 Planning 文档统一存放到 `doc/planning/`，并记录本次比分布局的根因、设计决策、实施 TODO 和验证结果。
- 根据真实发布提交补齐 `v1.6.0` 至 `v1.10.0` 之间缺失的版本日志。

### 验证

- 90 项自动化测试、ESLint 和 Cloudflare 生产构建通过。
- 浏览器验证桌面和 390px 手机布局；主行、半场比分与卡片中轴误差均小于 0.02px，三种赛果颜色及半场独立着色正确，控制台无错误。

## v1.10.0 · 2026-08-20

### 投注页自动获取赛果

- 进入投注页或展开日期时，自动检查当前已经渲染、日期未折叠、已经停售且尚无完整赛果的比赛，并批量获取赛果。
- 新增按比赛 ID 数组批量更新比赛的接口；后端匹配已有比赛并整批持久化赛果，未完整返回的比赛会保留在后续重试范围内。
- 比赛主行显示全场比分，副行以浅色仅显示半场比分，不额外增加玩法赛果行。

### 订单支付与批量操作

- 订单增加支付状态；历史订单缺少该字段时按未支付处理。订单从未支付变为已支付时会更新并锁定倍率，官方倍率暂不可用时保留订单原倍率继续支付。
- 新增统一的批量订单更新接口，一键判断并保存、更新倍率、锁定倍率、一键支付和一键结账均按订单 ID 数组整批处理；任一订单失败时整批回滚。
- 批量操作范围统一为当前筛选出的全部订单，不受页面渐进渲染数量影响；一键支付只处理未支付订单，一键结账只处理已支付且未结账订单。
- 一键支付会冻结订单的投注内容、倍数、串关和倍率；未支付订单显示“支付”，已支付订单显示“结账”，标题状态依次显示“已支付”或“已结账”。
- 一键结账增加二次确认 Popover，并展示订单数量、投入和预计入账等主要信息；一键支付也提供对应的批量入口与确认信息。
- 订单日期范围默认当天；账本与筛选统计按支付状态使用统一口径，未支付订单不计入已支付账务。

### 验证与发布

- 90 项自动化测试、ESLint、Cloudflare 生产构建、浏览器关键流程和批量接口事务验证通过，并完成生产部署。

## v1.9.0 · 2026-08-17

### 出票与奖金计算

- 同一场跨多个玩法选择时，按“每张票的同一场仅保留一个玩法”拆分出票；串关组合未包含该场时，按拆分出的票数重复计注和派奖。
- 单场与多关自由过关可同时选择，分别作为两个出票批次；多个多关关次仍合并为一个批次。
- 串关明细增加票数、组合数、注数汇总，并在每组明细中显示该组合对应的注数。
- 同步修正投入、当前奖金、奖金范围及封顶前奖金的计算口径，并补充跨玩法、单关与多关混合场景测试。

## v1.8.0 · 2026-08-17

### 订单批量判断与同步

- 一键判断改为只处理当前已经渲染、确实会被现有赛果改变的订单，并支持补齐已结账订单中尚未完整判断的比赛。
- 只有订单全部已选玩法都已有赛果，或已被明确标记成功/失败时，才将对应比赛视为判断完成。
- 批量结账和赛果汇总的操作范围与当前已渲染订单保持一致，避免渐进加载页面误操作尚未展示的数据。
- 订单写入后直接合并服务端返回的新增、更新和删除结果，同时增量维护订单总数、未结账数与账本，不再重新拉取固定上限的订单列表。

### 移动端日期选择

- 比赛日期、订单日期范围和订单创建时间的日期面板在小屏下改为纵向、可滚动布局，并限制在视口内。
- 移动端日期输入设为只读，避免软键盘遮挡；订单日期范围支持只填写开始或结束日期。

## v1.7.1 · 2026-08-04

### 预测单串关计算

- 预测模式不再被官方单场或过关销售标记阻断，只要已选项存在有效正赔率，就可以生成对应单场和串关选项。
- 无效或为零的赔率不参与注数、投入、命中奖金和奖金范围计算。
- 待开售比赛的比分等多选玩法也可以用于预测串关，并补充相应回归测试。

## v1.7.0 · 2026-08-04

### 体彩比赛数据完整性

- 常规刷新以完整比赛列表为主数据源，再用 calculator 返回的同场详细玩法和赔率覆盖，解决待开售或暂未返回赔率的比赛缺失问题。
- 固定奖金历史按比赛并发补取并在数据源之间复用，减少重复请求；完整列表不可用时仍可回退到 calculator 数据。
- 云端比赛刷新会向前端透传缓存与错误状态；缓存来自失败刷新时自动回退直连体彩接口，存在错误的刷新状态也不再受手动冷却时间阻断。

## v1.6.4 · 2026-08-03

### 订单操作反馈

- 保存预测单、添加手动订单、编辑订单和保存赛果增加明确的加载状态，提交期间锁定表单并阻止重复操作。
- 批量判断、更新倍率、锁定倍率、结账以及单笔撤回、删除等订单操作增加独立忙碌状态和成功反馈。
- 订单操作进行中统一禁用可能冲突的编辑、载入、复制和结果修改入口，降低重复点击造成的并发写入。

## v1.6.3 · 2026-08-03

### 云端订单迁移工具

- 订单重写脚本不再生成显式 `BEGIN TRANSACTION` / `COMMIT` 包装，改为使用 Wrangler D1 可直接执行的语句文件。
- 没有订单需要更新时生成安全的空操作说明，避免远端执行空 SQL 失败。

## v1.6.2 · 2026-08-03

### 大批量订单重写

- 提高订单重写脚本调用 Wrangler 时的输出缓冲区到 128 MB，避免远端数据量较大时因默认缓冲区不足中断。

## v1.6.1 · 2026-08-03

### D1 迁移兼容

- 模块化云端数据迁移改用 SQLite/D1 兼容的 `INSERT OR IGNORE ... SELECT` 写法，避免旧数据迁移时因冲突语法不兼容而失败。

## v1.6.0 · 2026-08-03

### 云端接口模块化

- 将原有 cloud bootstrap/personal 聚合接口拆分为用户、设置、比赛、订单和账务 REST 接口，并为前端增加对应 API client。
- 服务端业务逻辑拆分为 matches、orders、finance、settings 等 service；公共页面头部提取为复用组件，订单模型增加轻量化存储结构。
- 订单新增按 ID 的读取、编辑、删除、赛果保存、结账与撤回接口，并提供判断、更新倍率、锁定倍率和结账等批量接口。
- 比赛增加当前列表、主动刷新、快照保存和历史清理接口，支持 Worker 定时刷新与清理。

### 数据库迁移与运维

- 新增模块化 D1 表结构与迁移，将旧账号状态、订单、公共比赛和刷新状态转换到新模型。
- 增加云端订单重写工具和发布脚本支持，便于把旧订单规范化迁入新接口结构。

## v1.5.0 · 2026-08-03

### 云端订单写入语义

- 云端订单接口不再因为订单同步协议版本不同直接拒绝写入；旧页面仍只能按明确订单 ID 执行新增、更新或删除。
- 新版订单写入增加每单 `updatedAt` 冲突判断：同一订单被其他设备先修改时，后端返回“订单数据异常，请刷新获取最新数据”，前端保留编辑表单不关闭。
- 新增、编辑、赛果保存、倍率更新、结账、撤回、删除和导入订单都改为等待云端确认后再更新界面与账本，避免本地筛选子集被误当成完整云端订单。

### 云端账号数据来源

- 云端账号启动后严格使用 bootstrap 返回的订单、账本和设置，不再把个人数据安装进 localStorage，也不再从 localStorage 初始化云端账号数据。
- 退出登录只删除账号 session 并清理本地个人缓存，不再等待或触发全局 personal 同步；该同步的内容必须在具体操作时完成。
- 设置和账本只在云端账号内存中更新并通过 metadata 同步写云端；游客 Demo 仍然只写当前浏览器本地数据。

### 版本刷新提示

- 新增 `/api/version` no-store 接口；前端在启动、页面重新可见和定时检查时发现服务器版本变化，会提示刷新使用新版本。

### 订单录入交互修复

- 添加比赛后直接滚动弹窗内部的比赛列表，使首个尚未选择比赛的选择器稳定显示在列表中部。
- 保存或更新预测单的名称弹窗完成打开动画后，重新聚焦并选中订单名称输入框。

### 运维恢复

- 已按 `/Users/lewan666/Downloads/SMGR-订单-2026-07-31.json` 标准化订单结构并覆盖 `hen33769` 云端订单：88 个订单、80 个已结账、8 个未结账；保留账本字段不变。

## v1.4.2 · 2026-07-31

### 旧版删除队列保护

- 升级本地待同步队列版本；旧版队列无法证明每项修改都来自明确操作，升级时整批丢弃并以当前云端快照为基准。
- 云端订单写入接口增加同步协议版本校验，旧页面即使仍保留历史队列，也无法在刷新前修改云端订单。
- 退出登录不再把状态强制显示为“正在保存到云端”，也不会创建新的同步任务；只等待退出前已经发起的明确操作结束。
- 新增旧队列迁移回归测试，确保无法证明来源的历史订单与账本修改不会重放。

## v1.4.1 · 2026-07-31

### 订单按 ID 安全同步

- 订单新增、编辑、载入后更新、赛果保存、结账、撤回、删除和批量操作统一改为只提交本次操作涉及的明确订单 ID。
- 普通页面状态同步只处理收支和设置，不再根据当前订单数组缺少的项目推断云端删除，避免筛选结果或局部列表覆盖完整订单。
- JSON 合并只更新导入文件内的订单 ID；用户确认覆盖恢复时，才按覆盖前后的明确 ID 集合执行新增、更新和删除。
- 修复账号登录后保留投注草稿时的弹窗初始化时序，避免通过副作用追加无关状态更新。

### 验证

- 新增“10 个订单筛选出 5 个后编辑其中 1 个，云端仍保留全部 10 个 ID”的回归测试。
- 77 项测试、TypeScript 检查、定向 ESLint 检查和 Cloudflare 生产构建通过。

## v1.4.0 · 2026-07-30

### 订单列表渐进加载

- 订单卡片改为首批渲染 10 单，滚动接近列表底部时每批继续加载 10 单，减少大量订单同时渲染造成的页面卡顿。
- 切换订单日期、进度、状态、比赛队伍或比赛类型筛选时，列表重新从首批 10 单开始展示。
- 渐进加载只限制订单卡片的渲染数量；筛选结果数、筛选投入、筛选收入、筛选盈亏、赛果汇总以及批量锁定和结账仍使用完整筛选结果。
- 列表末端增加当前已展示数量提示，便于确认加载进度。

### 订单队伍与比赛类型筛选

- 订单页增加比赛队伍输入框，可按主队或客队名称模糊过滤订单，并忽略输入中的空格与大小写差异。
- 订单页增加与投注页一致的比赛类型标签，可筛选包含所选联赛类型的订单。
- 队伍和比赛类型只匹配订单中实际已投注的比赛，不会被订单快照中未选择的比赛误命中。
- 清除订单筛选时同步清空队伍和比赛类型条件。

### 比赛类型筛选行为

- 订单页和投注页的比赛类型默认均不选择；未选择任何类型时代表不限，选择后仅显示包含所选类型的订单或比赛。
- 类型标签增加选中状态和“不选则不限”提示，并支持键盘操作。
- 当列表中存在“世界杯”时固定排在首位，“欧冠”排在第二位，其余比赛类型保持接口或现有列表的原始顺序。

### 验证

- 新增比赛类型优先级、空类型不过滤、订单仅匹配实际投注比赛的回归测试。
- 75 项测试、TypeScript 检查、定向 ESLint 检查和 Cloudflare 生产构建通过。
- 浏览器验证了投注页类型选择/清空、订单队伍筛选/清空以及筛选统计恢复。

## v1.3.3 · 2026-07-30

### 订单筛选汇总

- “筛选盈亏”增加悬停明细，同时展示当前筛选范围内的投入与收入。
- 筛选收入沿用订单筛选账本口径，仅汇总已结账订单的中奖金额；盈亏继续按筛选收入减去筛选投入计算。
- 盈亏区域增加帮助型鼠标指针，明确提示可查看金额组成。

### 验证

- 完整测试、TypeScript 检查和 Cloudflare 生产构建通过。

## v1.3.2 · 2026-07-30

### 订单跨设备同步

- 页面打开时严格以云端订单、账本和设置为基准，再写入当前浏览器缓存。
- 移除旧版笼统“本地待上传”标记，避免电脑旧缓存覆盖手机刚保存的赛果判断。
- 只有已经持久化的精确增量任务，才会在启动时继续修改对应订单。
- 普通新增、编辑、判断赛果、结账和删除仍按订单 ID 增量同步，不替换整张云端订单表。
- 保留首次进入空账号时的本地数据迁移；JSON“覆盖恢复”仍是主动整体替换账号数据的入口。

### 订单赛果保留

- 重新保存现有订单时，保留该订单已经判断的命中结果，并按当前仍存在的投注选项过滤。
- 新建订单不再继承投注页临时命中状态，避免把未确认结果写入新订单。

### 验证

- 新增远端优先、精确增量恢复和首次空账号迁移的回归测试。
- 完整测试、TypeScript 检查和 Cloudflare 生产构建通过。

## v1.3.1 · 2026-07-29

- 提高弹窗可用高度，减少订单编辑和玩法选择时的无效留白。
- 优化小屏与桌面端弹窗滚动体验。

## v1.3.0 · 2026-07-29

- 完善订单编辑，支持调整名称、创建时间、倍数、串关和订单赔率快照。
- 支持将订单导出为可再次手动导入的文本格式。
- 订单按创建时间稳定排序，手动订单中的比赛按比赛日期与开赛时间排序。
- 优化实时中奖奖金范围：同场已有命中项时，不再把该场其他互斥选项重复计入。
- 完善订单导入合并，同 ID 订单以导入内容更新，并保留文件未提供的可选字段。

## v1.2.0 · 2026-07-29

- 手动订单支持单场、单关、数字关次和中文关次等多种串关写法。
- 订单编辑器支持直接调整串关方式，并同步修正累计投入。
- 增加串关输入的追加、排序、去重与边界校验。

## v1.1.4 · 2026-07-29

- 云端启动数据会从赔率历史中补齐当前倍率，避免有效历史倍率显示为空。

## v1.1.3 · 2026-07-29

- 公共比赛改为按比赛 ID 增量写入，不再每次清空整张比赛表。
- 新接口数据缺少当前倍率时，保留云端已有有效倍率与赔率历史。
- 自动清理超出保留窗口的旧比赛。

## v1.1.2 · 2026-07-29

- 账号订单同步从整表替换升级为按订单 ID 增量新增、修改与删除。
- 引入云端 revision 冲突检测，降低多设备同时操作时互相删除订单的风险。
- 同步失败期间的多次本机修改会合并为最后一次明确操作。

## v1.1.1 · 2026-07-28

- 优化移动端玩法选择、赔率展示和页面头部响应式布局。
- 调整比分等更多玩法的分组与弹窗交互。

## v1.1.0 · 2026-07-28

- 正式版与游客 Demo 分离：正式版使用账号云同步，游客数据仅保存在当前浏览器。
- 公共比赛对未登录用户开放，登录后订单、收支和设置按账号同步。
- JSON 数据工具区分新增合并与覆盖恢复。

## v1.0.1 · 2026-07-28

- 建立 GitHub 推送、Cloudflare Worker、D1 迁移和正式域名健康检查的一键发布流程。
- 明确正式版以 D1 为云端来源，浏览器缓存用于本地回退。

## 早期版本 · 2026-07-23 至 2026-07-28

- 完成竞彩足球五类玩法、串关、倍数、投入和奖金范围计算。
- 增加订单保存、命中判断、失败状态、结账与账本。
- 接入体彩官方比赛、赔率历史、赛果解析和赛事前瞻。
- 增加 OCR、手动比赛、JSON 导入导出、联赛筛选与主题设置。
- 从本地单页应用逐步迁移到 Cloudflare Worker 与 D1 云同步架构。
