# 订单联赛命中率柱状图 Planning

日期：2026-09-10

关联 Research：`doc/planning/2026-09-10-order-league-accuracy-chart-RESEARCH.md`

## 目标

- 在订单筛选卡“比赛类型”标题旁增加联赛命中率入口和两行汇总。
- 基于 `filteredSavedSlips` 按联赛汇总命中、未命中、未确认和命中率，并用 ECharts 柱状图展示。
- 支持联赛多选、“不统计比分”、“重复计数”和“固定 Max”，保持桌面端与移动端可用。

## 实施方案

1. 新增联赛正确率纯函数，按“订单 × 比赛 × 已选玩法”分类，兼容 `hits`、`resultValues`、`failedMatches` 和旧订单不完整结果。
2. 默认重复统计每个订单中的预测；关闭“重复计数”后按“标准化比赛 ID + 玩法 + 已选项 ID 集合”去重，并在重复记录间优先采用证据更明确的状态。
3. 正确率使用 `命中 / (命中 + 未命中)`；未确认不进入分母，没有已确认结果时返回 `null` 并显示“—”。
4. 新增联赛正确率弹窗，联赛默认全选、支持清空；“不统计比分”和“重复计数”默认开启，“固定 Max”默认关闭。
5. 使用柱状图展示正确率，柱顶显示百分比，tooltip 展示命中、未命中、未确认；y 轴最小值固定 0%，固定 Max 开启时最大值为 100%，关闭时使用当前数据最大值（全为 0% 时以 1% 避免零区间）。
6. 在“比赛类型”标题旁加入 `BarChartOutlined` 入口，传入 `filteredSavedSlips`，不受订单列表分批渲染数量影响。
7. 增加聚合纯函数测试，并完成完整测试、Lint、构建及桌面/移动浏览器验证。

## 接口、数据与兼容

- 不新增 API，不修改订单模型、云端接口或 D1；账号版仍只统计当前查询已加载的最多 500 条候选订单经过全部前端筛选后的结果。
- 开关和联赛选择只存在于弹窗本地状态，不持久化到账户设置或浏览器存储。
- 空联赛不生成柱；同场其他玩法在“不统计比分”开启时仍正常统计。
- 去重冲突时状态证据优先级为命中、未命中、未确认；具体命中值比整场失败标记更可信。
- 发布按新增功能使用 minor 版本 `v1.25.0`，同步更新 `package.json`、`package-lock.json` 与 `UPDATE.md`；无数据库迁移或生产数据重写。

## TODO

- [x] 完成 Research 并确认统计数据集、重复计数与正确率口径。
- [x] 实现联赛正确率聚合纯函数。
- [x] 实现联赛选择与三个 checkbox 控制的 ECharts 柱状图弹窗。
- [x] 在订单筛选卡增加趋势入口并接入 `filteredSavedSlips`。
- [x] 完成响应式样式与无数据状态。
- [x] 增加聚合函数自动化测试并接入 `npm test`。
- [x] 运行 `git diff --check`、`npm test`、`npm run lint`、`npm run build`。
- [x] 完成桌面端、移动端及浏览器控制台验证。
- [x] 回填实施结果与验证结果。
- [x] 将用户侧“正确率”统一改名为“命中率”。
- [x] 在图表入口增加整体命中率和成功订单数量汇总。
- [x] 移除柱状图的滚轮和滑块缩放功能。
- [x] 在每根柱顶的命中率下方增加该联赛已确认场次。
- [x] 将每根柱颜色与设置页的联赛标签颜色保持一致。
- [x] 将图表入口首行修正为订单命中率，排除“有希望”的未确认订单。
- [x] 在联赛选择器首项增加“全选”，并支持点击柱子取消对应联赛。
- [x] 调整图表联赛顺序：七个指定联赛置顶，其余按名称升序。
- [x] 将联赛命中率弹窗最小高度设为 660px。
- [x] 验证新增汇总和无缩放柱状图的桌面端与移动端布局。

## 实施结果

- 新增 `app/league-accuracy.ts`，统一处理玩法级状态分类、正确率计算、比分排除与重复预测去重。
- 新增 `app/LeagueAccuracyModal.tsx`，提供联赛多选、三个统计开关、柱顶百分比、统计 tooltip 与异常重试，图表不提供缩放功能。
- 在订单页“比赛类型”标题旁增加图表入口，并将完整的 `filteredSavedSlips` 传给弹窗。
- 图表入口首行展示订单命中率，按 `成功订单 ÷（成功订单 + 失败订单）` 计算，副行展示当前筛选结果中的成功订单数量；该汇总不受弹窗玩法级统计开关影响。
- 每根柱顶首行展示命中率，副行展示该联赛 `命中 + 未命中` 的已确认场次。
- 每根柱通过 `getLeagueTagColor` 复用设置页的联赛标签颜色，颜色设置变化后同步更新图表。
- 联赛选择器提供置顶的“全选”快捷项，柱子点击可取消对应联赛；指定七个联赛固定置顶，其余联赛按名称升序。
- 联赛命中率弹窗通过当前 Ant Design 的 `.ant-modal-container` 设置 660px 最小高度。
- 新增响应式样式和 9 项聚合测试；无联赛、联赛选择清空及无已确认结果均有明确展示。

## 验证结果

- `git diff --check`：通过。
- `npm test`：通过，146 项测试全部成功。
- `npm run lint`：通过，0 个错误；仅有 3 条改动前已存在的 `<img>` 优化警告。
- `npm run build`：通过；保留既有的图标 barrel 优化与大 chunk 提示。
- `tsc --noEmit`：仍被本次未修改的既有类型问题阻断（`FinanceTrendModal` 的 ECharts `triggerOn` 类型，以及 `tests/calculator.test.ts` 的 `failedMatches` fixture 类型）。
- 桌面浏览器：验证入口订单命中率、默认开关、柱顶命中率与场次、tooltip、联赛清空和全选、点击柱子取消联赛、固定优先级排序、重复计数、比分统计、固定 Max、无缩放图表及逐柱联赛标签颜色。
- 移动浏览器（390×844）：验证入口两行布局、弹窗尺寸、控件换行、联赛标签折叠及全部柱直接展示；干净页面控制台无错误。
- 后续订单级汇总与交互优化按 minor 版本 `v1.26.0` 发布；仍不涉及数据库迁移或生产订单重写。
