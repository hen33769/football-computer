# 订单已支付筛选与筛选收入 Planning

## 实施方案

- [x] 扩展订单筛选类型和 UI 选项，在“订单状态”多选中加入“已支付”；前端本地筛选按支付状态判断。
- [x] 更新订单 API / 服务端筛选，把 `paid` 作为支付字段条件，并保证它与成功 / 有希望 / 失败多选时按 OR 组合，其他条件仍按 AND 组合。
- [x] 新增订单页专用的筛选收入汇总：仅已支付成功订单计入，优先采用已结账奖金与当前奖金中的较大值，从而覆盖未结账成功订单；保持全局 `orderLedgerTotals` 和财务趋势口径不变。
- [x] 增加已支付云端查询组合和未结账成功、未支付成功订单收入的回归测试。
- [x] 运行 `npm test`、`npm run lint`、`npm run build`，检查未覆盖的工作区改动不被修改。

## 接口与数据结构

- `OrderStatusFilter` 增加字面量 `paid`；`OrderQuery.statuses` 继续通过 `status` 查询参数传输。
- D1 `user_orders` 表不变，服务端将 `paid` 映射到既有 `payment_status` 列。

## 兼容与发布影响

- 旧客户端未传 `paid` 时行为不变；服务端仍忽略未知状态值。
- 不新增数据库迁移，不改变累计收入、支出和趋势接口。
- 本次为订单筛选与摘要行为调整，若进入部署流程应按仓库约定提升 minor 版本并同步 `UPDATE.md`；当前只实现和验证，不提交、推送或部署。

## 验证

- 单元测试验证：已支付筛选 SQL 能与订单状态筛选组合；已支付未结账成功订单会计入筛选收入，未支付 / 有希望 / 失败订单不会计入。
- 完成完整测试、Lint 与 Cloudflare 构建。
