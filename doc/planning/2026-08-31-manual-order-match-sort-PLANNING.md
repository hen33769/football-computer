# 手动添加订单比赛选项排序 Planning

## 实施步骤

- [x] 调整手动订单比赛比较器的排序键和兜底逻辑。
- [x] 更新排序回归测试，覆盖同日场次号与开赛时间顺序相反的情况。
- [x] 运行 `npm test`、`npm run lint`、`npm run build`。
- [x] 用浏览器打开手动添加订单弹窗，检查桌面/移动端选项顺序和控制台。

## 接口与数据结构

- 不新增 API、D1 表、迁移或数据字段。
- 仅调整前端下拉选项的展示排序。

## 兼容策略与发布影响

- 比赛选择值仍使用标准化比赛 ID，排序变化不影响订单保存内容。
- 这是既有界面排序的 bug 修复，若进入发布流程按 patch 版本处理。
- 本轮只修改和验证，不提交、推送或部署。
