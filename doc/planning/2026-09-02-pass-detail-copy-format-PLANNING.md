# 串关明细排序与日期比赛复制格式 Planning

## TODO

- [x] 增加总倍率降序、稳定排序工具并接入串关明细分组。
- [x] 增加日期比赛复制行格式化工具并接入投注页面复制功能。
- [x] 补充回归测试，覆盖排序与让球格式边界。
- [x] 运行 `npm test`、`npm run lint`、`npm run build`。
- [x] 用浏览器检查投注页面复制按钮、桌面/移动端页面和明细展示，控制台无错误。

## 实施步骤

1. 在 `app/calculator.ts` 增加 `sortPassMultiplierDetails`，使用 `multiplier` 降序，并用原数组索引作为相同倍率时的稳定兜底。
2. 在 `app/FootballApp.tsx` 的每个串关分组内调用该排序工具；不调整 `passes` 的分组顺序和计算输入。
3. 新增比赛复制格式化模块，读取 `rqspf.handicap`：负值为“主让绝对值”、正值为“客让绝对值”、0 为“平手”、缺失值省略盘口括号。
4. 让 `copyMatchesForDate` 保持原日期首行和提示逻辑，仅替换比赛行格式。
5. 在现有 Node 测试中覆盖上述纯函数，并按风险运行完整验证命令。

## 接口与数据结构变化

- 不新增 API、D1 表、迁移或持久化字段。
- 新增的排序与文本格式化函数均为前端纯函数；`MatchItem` 和 `PassMultiplierDetail` 结构不变。

## 兼容策略与发布影响

- 只改变明细的展示顺序和复制文本，不影响订单保存、投注计算、结果判断或比赛 ID 匹配。
- 旧比赛快照缺失盘口时仍可复制，只是不附加盘口文案。
- 本轮只修改和验证，不提交、推送或部署；若后续进入发布流程，按行为修复使用 patch 版本并同步 `UPDATE.md`、版本号。

## 验证方式

- 单元测试验证倍率排序的降序和稳定性，以及 `-2`、`+1`、`0`、缺失盘口的复制行结果。
- 运行 `npm test`、`npm run lint`、`npm run build`。
- 已用本地开发服务检查桌面端和移动端视口的复制按钮、投注汇总和明细抽屉，控制台无错误；复制成功提示已出现，具体文本由纯函数回归测试覆盖。

## 验证结果

- `npm test`：124 项通过。
- `npm run lint`：通过，3 个既有 `<img>` 性能警告，无错误。
- `npm run build`：通过；仅有既有的图标导入优化、包体积和动态路由分类提示。
