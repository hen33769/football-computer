# 队伍名称优先级与客队展示 Planning

## 实施计划

- [x] 更新队伍名称展示解析，使完整配置始终按激活 1、激活 2 输出。
- [x] 为名称组件增加别名位置控制，修正客队为“logo 主名 别名”。
- [x] 补充激活 1、激活 2、未激活接口名称的回归测试，运行测试、lint、build。
- [x] 使用桌面端和 390px 移动端浏览器检查投注卡片及无图标兼容性。
- [x] 将名称组最少名称数调整为 1 个，支持单名称配置仅绑定队伍 Logo。
- [x] 在添加队伍按钮左侧增加名称搜索框，按任意配置名称过滤队伍信息列表。
- [x] 为联赛标签颜色卡片增加添加联赛按钮和默认颜色保存逻辑。
- [x] 为联赛标签颜色、队伍信息两个 Card 增加纯图标折叠按钮，并修正队伍删除按钮危险样式。
- [x] 增加名称行拖拽排序，保存时清理空名称并规范仅激活 2 的配置。
- [x] 持久化队伍组显示顺序；编辑保留原位置，新建组放在列表顶部，不使用更新时间或创建时间定位。

## 接口与数据结构

- 不新增接口；沿用已有队伍信息配置公共 GET 和管理员 POST/PATCH/DELETE，新增 D1 `display_order` 字段保存队伍组显示顺序。
- 仅调整 `resolveTeamNameDisplay`/`buildTeamNameIndex` 的展示结果与 `TeamNameWithAlias`/`TeamNameWithIcon` 的渲染参数，并放宽名称组校验为至少 1 个已输入名称；设置页搜索和拖拽排序仅为前端编辑行为，不新增接口或名称组接口。
- 联赛新增复用 `withLeagueTagColor` 和现有设置持久化链路；折叠状态只存在前端，不新增接口或数据字段。
- `display_order` 迁移时按原接口列表顺序初始化；新增队伍组分配到列表顶部，编辑和名称拖拽不修改队伍组显示顺序。

## 发布影响

- 属于既有投注页展示行为修正，发布时按 patch 版本处理；本次实现阶段不提交、推送或部署，除非另行收到发布指令。

## 验证结果

- `npm test`：122 项全部通过，覆盖激活 1、激活 2、未激活接口名称按配置优先解析、全部未激活配置及保存规范化、队伍组位置保持。
- `npm run lint`：0 errors；保留既有 3 个 data URL `<img>` 性能 warning。
- `npm run build`：通过；保留项目已有的图标 barrel export、大 chunk 和动态 API 分类 warning。
- 本地投注页：桌面端 30 张比赛卡、390px 移动端均正常加载，无图标时不生成 logo 节点，控制台无 error。
- 单名称和全部未激活配置校验通过；空名称自动清理；仅激活名称 2 会在保存时转为激活名称 1。
- 设置页搜索框使用 `SearchOutlined` 前缀并与添加按钮保持同级布局。
- 联赛新增入口、两个 Card 折叠交互和删除按钮样式已完成页面验证；桌面及 390px 移动端无横向溢出，控制台无错误。
