# SMGR 开发与发布约定

本文件适用于仓库根目录及其全部子目录。处理本仓库任务时，应先阅读并遵守本文件；若更深层目录以后增加了 `AGENTS.md`，则同时遵守更具体的约定。

## 开发流程

### 1. Research

- 新功能、行为调整或涉及多个模块的改动，先深入阅读相关代码、测试、数据库迁移和文档，确认现有实现、数据流与影响范围。
- Research 结论写入 `doc/planning/<YYYY-MM-DD>-<需求名称>-RESEARCH.md`，简洁记录相关文件、现有行为、约束、风险和待确认问题。
- 需求存在会改变实现方向的歧义时，先向开发者确认。开发者明确要求“先分析 / research”时，在双方对结论达成共识前不要开始实现。
- 纯文档、明确的小范围修复可以省略独立 Research 文档，但仍必须先读懂相关代码或资料。

### 2. Planning

- 根据 Research 制定实现计划，写入 `doc/planning/<YYYY-MM-DD>-<需求名称>-PLANNING.md`。
- 计划应包含实施步骤、接口或数据结构变化、数据库迁移、兼容策略、验证方式以及发布影响。
- 在同一份 Planning 文档中维护 TODO；实现过程中逐项勾选。
- 开发者要求先审阅方案，或计划中仍有关键选择未确认时，等待确认后再编码。

### 3. Implementation

- 未完整读懂关联代码前不得开始实现；优先复用现有 API client、server service、业务模型和 UI 组件。
- 修改必须保持接口、前端状态、D1 数据和测试口径一致。数据库结构变化新增迁移文件，不要改写已经发布的迁移。
- 保留工作区内与当前任务无关的改动，不覆盖或回退开发者的内容。
- 需要分支时使用 `codex/` 前缀；除非开发者明确要求，不自行提交、推送或部署。

### 4. Verification

- 按风险运行相关测试；发布前至少运行 `npm test`、`npm run lint` 和 `npm run build`。
- 页面交互变化需要用浏览器验证关键桌面端与移动端流程，并检查控制台错误。
- API、订单批量写入、账本或 D1 迁移变化必须验证成功、失败回滚、权限与并发冲突路径。
- 不得把“代码已写完”视为完成；验证结果应在交付说明或更新日志中明确记录。

## 更新日志与部署（强制）

- `UPDATE.md` 是本仓库唯一的版本更新日志，按版本从新到旧排列。
- **每次部署前都必须先把本次全部改动写入 `UPDATE.md`，然后依次执行提交、推送、部署。固定顺序为：更新日志与版本 → 提交 → 推送 → 部署；不得先部署或先推送、事后再补日志。**
- 仅执行提交或推送、明确不部署时，不需要更新 `UPDATE.md`，也不需要仅因此调整版本号。
- 一旦进入部署流程，更新日志必须与准备发布的代码、版本号放在同一个发布提交中并一起暂存；即使本次只有修复、样式、文案、配置、迁移或文档改动，也不能省略。
- 每个版本使用 `## vX.Y.Z · YYYY-MM-DD` 标题。已存在当前版本时补充或修正该节，不重复创建同名版本；新版本放在最前。
- 日志描述实际用户行为、接口、数据迁移、兼容性和重要验证结果，不只复述提交标题。补历史日志时必须核对对应发布提交的真实 diff。
- 发布提交前检查 `package.json`、`package-lock.json`、`app/AppVersion.tsx` 派生的页面版本和 `UPDATE.md` 标题一致。
- 部署前按改动类型更新版本：
  - `patch`：问题修复、样式、文案或不改变主要能力的小调整。
  - `minor`：新增、删除功能或调整既有行为；中间位加一并将 patch 归零。
  - `major`：不兼容或大范围重构；主版本加一并将其余位归零。
- 正式发布优先使用 `make release-patch`、`make release-minor` 或 `make release-major`；发布前可用 `make release-dry-run` 核对参数。

## 项目约定

- 页面与交互主要位于 `app/FootballApp.tsx` 及其拆分组件；API client 位于 `app/api-client/`，路由位于 `app/api/`，服务端业务逻辑位于 `app/server/`。
- 订单更新以订单 `id` 匹配，保留 `updatedAt` 并遵守并发冲突检查；批量操作必须保持后端事务的整批成功或整批失败语义。
- 比赛写入以标准化后的比赛 `id` 匹配，不用列表位置或页面顺序作为持久化依据。
- 云端金额以整数分存储和计算，页面以元展示；转换统一复用现有 money/finance 工具。
- 正式 D1 数据与用户订单属于高风险数据。执行远端迁移、重写、删除、覆盖恢复或生产部署前，必须确认目标环境和操作范围。

## 常用命令

- 本地开发：`npm run dev`
- 测试：`npm test`
- 代码检查：`npm run lint`
- Cloudflare 构建：`npm run build`
- 本地 D1 迁移：`npm run db:migrate:local`
- 发布预检：`make release-dry-run`
